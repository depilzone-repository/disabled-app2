import 'package:flutter/cupertino.dart';

class ListaBox extends StatelessWidget{
    const ListaBox({super.key});

    @override
    Widget build(BuildContext context){
      return const Column(
        children: [
          Text("Hola")
        ],
      );
    }

}